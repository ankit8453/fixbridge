# Admin console (placeholder)

Nothing here yet. The operations/admin web console is built in **Phase 11**.

It will talk to the API's `/api/v1/admin` module (currently an empty stub at
[apps/api/src/modules/admin/](../api/src/modules/admin/)).

Do not add code here before Phase 11 — see the scope discipline note in the root
[README](../../README.md).
