// Stub for Phase 7 — slots, booking lifecycle and the start/end OTP handshake. Filled then, not now.
// All Prisma / raw-SQL access for this domain lives here — nothing else touches the DB.

export {};
